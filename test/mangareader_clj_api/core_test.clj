(ns mangareader-clj-api.core-test
  (:require [clojure.test :refer :all]
            [mangareader-clj-api.api.handler-should]
            [mangareader-clj-api.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 1 1))))

(comment
  (run-tests
    'mangareader-clj-api.api.handler-should
    )
  )
